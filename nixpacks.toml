[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "cd backend && pip install --no-cache-dir -r requirements-backend.txt"
]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"